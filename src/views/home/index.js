import React from 'react';
import { Typography } from '@mui/material';

const home = () => {
  return (
    <div>
      <Typography variant="h1" component="h2">
        Hola !
      </Typography>
      <Typography variant="h1" component="h2">
        Selecciona una de las opciones de el menu de la izquierda
      </Typography>
    </div>
  );
};

export default home;
